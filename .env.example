# =============================================================================
# Environment Variables
#
# Copy to .env for local development:  cp .env.example .env
# All variables have defaults — the app starts with zero config.
# =============================================================================

# -- App metadata --------------------------------------------------------------
# APP_NAME=pysonica
# DEBUG=false
# VERSION=0.1.0
# ENVIRONMENT=local                # local | staging | production

# -- Database ------------------------------------------------------------------
# Full async URL. The +asyncpg driver is required.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/app

# Connection pool tuning (defaults are fine for most workloads)
# DATABASE_POOL_SIZE=5             # Persistent connections
# DATABASE_POOL_OVERFLOW=10        # Additional connections above pool_size

# -- Logging -------------------------------------------------------------------
# LOG_LEVEL=INFO                   # DEBUG | INFO | WARNING | ERROR | CRITICAL
# LOG_JSON_FORMAT=false            # true → JSON lines (production), false → console (dev)
