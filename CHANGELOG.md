# Changelog

Auto-generated by [commitizen](https://commitizen-tools.github.io/commitizen/). Do not edit manually.

## v0.1.0 (Unreleased)

### Features

- Configuration module (Pydantic Settings, `@lru_cache`, `SecretStr`)
- Structured logging (structlog + stdlib integration, correlation IDs)
- Database module (async SQLAlchemy 2.0, session-per-request, `pool_class` param)
- Error handling (domain exception hierarchy, global handlers, ASGI catch-all middleware)
- Health checks (`/health/live`, `/health/ready` with composable conditions)
- Security headers middleware (OWASP Top 10 coverage)
- Retry utilities (tenacity with full jitter, typed wrapper)
- CLI framework (typer with exit codes, SIGTERM handling)
- API versioning skeleton (`/api/v1/`)
- Multi-stage Dockerfile (uv, non-root, `--no-editable`, bind-mount layers)
- CI pipeline (lint, typecheck, test, security scan)
- Pre-commit hooks (ruff, pyright, detect-secrets, commitizen, squawk)
- Migration stairway test (upgrade→downgrade→upgrade per revision)
- Import-linter enforcement (no cross-module imports)
- Rollback-based test isolation (NullPool, real PostgreSQL)